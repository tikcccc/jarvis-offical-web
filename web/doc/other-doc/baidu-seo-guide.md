# 百度 SEO 優化指南

本文檔提供百度搜索引擎優化的完整設置和提交指南。

## 目錄
- [百度特定優化](#百度特定優化)
- [百度站長平台設置](#百度站長平台設置)
- [Sitemap 提交](#sitemap-提交)
- [移動端優化](#移動端優化)
- [常見問題](#常見問題)

## 百度特定優化

### 1. Robots.txt 配置

已為百度及其他中文搜索引擎配置專門規則：

```
User-agent: Baiduspider      # 百度爬蟲
User-agent: Sogou web spider # 搜狗爬蟲
User-agent: 360Spider        # 360搜索爬蟲
Allow: /
Disallow: /studio
Disallow: /api/
```

**訪問：** `https://isbim.com/robots.txt`

### 2. Meta 標籤優化

每個頁面自動包含百度優化的 meta 標籤：

```html
<!-- 語言標記（百度偏好 zh-CN） -->
<link rel="alternate" hreflang="zh-CN" href="https://isbim.com/zh/..." />

<!-- Open Graph locale（百度格式） -->
<meta property="og:locale" content="zh_CN" />

<!-- 設備適配提示 -->
<meta name="applicable-device" content="pc,mobile" />

<!-- 電話號碼格式檢測 -->
<meta name="format-detection" content="telephone=no" />

<!-- 百度站長驗證（註冊後添加） -->
<!-- <meta name="baidu-site-verification" content="code-from-baidu" /> -->
```

### 3. Sitemap 格式

現有 sitemap.xml 已完全兼容百度：
- ✅ 標準 XML 格式
- ✅ 包含 `<lastmod>` 時間戳
- ✅ 優先級和更新頻率
- ✅ 多語言 hreflang 標記

## 百度站長平台設置

### 步驟 1: 註冊百度站長工具

1. 訪問：https://ziyuan.baidu.com/
2. 使用百度帳號登錄（需要中國手機號）
3. 點擊「用戶中心」→「站點管理」→「添加網站」

### 步驟 2: 驗證網站所有權

選擇驗證方法（推薦 Meta 標籤驗證）：

**Meta 標籤驗證：**
1. 百度提供驗證代碼：`<meta name="baidu-site-verification" content="codexxx" />`
2. 添加到 `src/lib/seo.ts`：

```typescript
// 在 generatePageMetadata 函數中的 other 字段
other: {
  "baidu-site-verification": "codexxx", // 替換為實際代碼
  "applicable-device": "pc,mobile",
  "format-detection": "telephone=no",
},
```

3. 部署到生產環境
4. 在百度站長平台點擊「驗證」

**文件驗證（備選）：**
1. 下載百度提供的驗證文件（如 `baidu_verify_xxx.html`）
2. 放到 `public/` 目錄
3. 訪問 `https://isbim.com/baidu_verify_xxx.html` 確認可訪問
4. 在百度站長平台點擊「驗證」

### 步驟 3: 提交 Sitemap

驗證成功後：

1. 進入「站點管理」→ 點擊您的網站
2. 左側菜單選擇「數據引入」→「鏈接提交」
3. 選擇「sitemap」
4. 提交 URL：`https://isbim.com/sitemap.xml`
5. 點擊「提交」

**預期結果：**
- 百度會開始定期抓取 sitemap
- 在「索引量」中可以看到收錄數量
- 通常 1-2 週開始有明顯收錄

### 步驟 4: 設置自動推送（可選）

百度提供自動推送 API，可在用戶訪問時主動推送 URL：

```html
<!-- 添加到 layout.tsx 的 <head> 中 -->
<script>
(function(){
    var bp = document.createElement('script');
    var curProtocol = window.location.protocol.split(':')[0];
    if (curProtocol === 'https') {
        bp.src = 'https://zz.bdstatic.com/linksubmit/push.js';
    }
    else {
        bp.src = 'http://push.zhanzhang.baidu.com/push.js';
    }
    var s = document.getElementsByTagName("script")[0];
    s.parentNode.insertBefore(bp, s);
})();
</script>
```

## 移動端優化

百度非常重視移動端體驗，確保：

### 1. 響應式設計
✅ 已實現（使用 Tailwind CSS breakpoints）

### 2. Meta Viewport
✅ 已在 layout.tsx 中配置：
```html
<meta name="viewport" content="width=device-width, initial-scale=1" />
```

### 3. 移動適配聲明
✅ 已添加到每個頁面：
```html
<meta name="applicable-device" content="pc,mobile" />
```

### 4. 移動 Sitemap（可選）

如果需要單獨的移動站點地圖，可創建 `src/app/sitemap-mobile.xml/route.ts`。

## Sitemap 提交

### 自動提交（推薦）

**方法 1: 通過 robots.txt**
✅ 已實現 - sitemap 位置已在 robots.txt 中聲明：
```
Sitemap: https://isbim.com/sitemap.xml
```

**方法 2: 主動推送 API**

當內容更新時，可使用百度主動推送 API：

```typescript
// 在 Sanity webhook 或 revalidation 時調用
async function pushToBaidu(urls: string[]) {
  const site = 'https://isbim.com';
  const token = process.env.BAIDU_PUSH_TOKEN; // 從百度站長獲取

  const response = await fetch(
    `http://data.zz.baidu.com/urls?site=${site}&token=${token}`,
    {
      method: 'POST',
      headers: { 'Content-Type': 'text/plain' },
      body: urls.join('\n'),
    }
  );

  return response.json();
}
```

### 手動提交

1. 訪問百度站長平台
2. 「數據引入」→「鏈接提交」→「手動提交」
3. 粘貼最多 2000 個 URL（每行一個）
4. 點擊提交

## 驗證 SEO 設置

### 檢查 Robots.txt
```bash
curl https://isbim.com/robots.txt

# 應該看到：
User-agent: Baiduspider
Allow: /
Disallow: /studio
...
Sitemap: https://isbim.com/sitemap.xml
```

### 檢查 Sitemap
```bash
curl https://isbim.com/sitemap.xml

# 應該看到 XML 格式的 URL 列表
```

### 檢查頁面 Meta 標籤
```bash
curl https://isbim.com | grep -i baidu
curl https://isbim.com/zh/about-us | grep "zh-CN"
```

## 性能監控

### 百度統計（百度 Analytics）

1. 註冊：https://tongji.baidu.com/
2. 創建新站點
3. 獲取跟踪代碼
4. 添加到 `layout.tsx`：

```html
<script>
var _hmt = _hmt || [];
(function() {
  var hm = document.createElement("script");
  hm.src = "https://hm.baidu.com/hm.js?your-site-id";
  var s = document.getElementsByTagName("script")[0];
  s.parentNode.insertBefore(hm, s);
})();
</script>
```

### 關鍵指標追蹤

在百度站長平台監控：
- **索引量**：已收錄的頁面數
- **抓取頻次**：百度訪問網站的頻率
- **抓取異常**：404、超時等錯誤
- **移動適配**：移動端優化評分
- **網站體檢**：安全性和可訪問性

## 常見問題

### Q: 為什麼百度收錄很慢？

A: 百度收錄通常比 Google 慢，原因：
1. 新站點需要建立信任（1-3 個月）
2. 缺少外部鏈接
3. 內容質量評估需要時間

**解決方案：**
- 持續更新高質量中文內容
- 獲取可信中文網站的外鏈
- 使用百度主動推送 API
- 在百度知道、貼吧等平台增加曝光

### Q: sitemap 提交後沒反應？

A: 檢查：
1. sitemap 格式是否正確（訪問 `/sitemap.xml`）
2. robots.txt 是否允許抓取
3. 網站是否可從中國訪問（使用中國 VPN 測試）
4. 是否有大量 404 錯誤

### Q: 需要為簡體中文和繁體中文分別優化嗎？

A:
- 百度主要服務簡體中文市場
- 如果目標台灣/香港市場，考慮添加繁體中文頁面
- 使用 `hreflang="zh-Hant"` 標記繁體頁面
- 使用 `hreflang="zh-Hans"` 標記簡體頁面

### Q: 百度站長驗證碼如何獲取？

A:
1. 必須使用中國手機號註冊百度帳號
2. 如果在海外，可使用虛擬中國手機號服務
3. 或委託中國合作夥伴協助註冊

### Q: 移動端適配問題？

A: 百度對移動端有嚴格要求：
- 確保響應式設計
- 避免 Flash、彈窗
- 頁面加載速度 < 3 秒
- 字體大小適合移動設備
- 按鈕足夠大，易於點擊

## 百度 vs Google SEO 差異

| 特性 | Google | 百度 |
|------|--------|------|
| **收錄速度** | 快（數小時-數天） | 慢（數週-數月） |
| **更新頻率** | 實時 | 相對慢 |
| **外鏈權重** | 高 | 非常高 |
| **內容原創性** | 重要 | 極其重要 |
| **移動優先** | 是 | 是 |
| **HTTPS** | 加分項 | 加分項 |
| **網站速度** | 重要 | 重要 |
| **中文分詞** | 較弱 | 強 |
| **.cn 域名** | 無優勢 | 有優勢 |

## 最佳實踐總結

### ✅ 已完成
- Robots.txt 百度爬蟲配置
- Sitemap.xml 生成（兼容百度）
- Meta 標籤優化（zh-CN, applicable-device）
- 多語言 hreflang 支持
- 移動端響應式設計

### 📋 待完成（部署後）
1. 註冊百度站長平台
2. 驗證網站所有權
3. 提交 sitemap.xml
4. 設置百度統計
5. 配置主動推送 API（可選）
6. 監控收錄和排名

### 🚀 優化建議
1. **內容策略**：
   - 持續發布高質量中文內容
   - 關注建築/AI/BIM 領域關鍵詞
   - 避免內容重複

2. **外鏈建設**：
   - 中國行業網站
   - 百度百科、知道
   - 中文博客和論壇

3. **技術優化**：
   - CDN 使用中國節點（如阿里雲、騰訊雲）
   - ICP 備案（如有中國伺服器）
   - 確保從中國訪問速度快

4. **本地化**：
   - 使用簡體中文
   - 符合中國用戶習慣的設計
   - 中國社交媒體整合（微信、微博）

## 相關資源

- **百度站長平台**：https://ziyuan.baidu.com/
- **百度統計**：https://tongji.baidu.com/
- **百度 SEO 指南**：https://ziyuan.baidu.com/college/courseinfo?id=267
- **移動優化指南**：https://ziyuan.baidu.com/college/courseinfo?id=156

---

**注意**：百度 SEO 是一個持續過程，需要耐心和堅持。建議每月檢查站長平台數據，並根據反饋調整策略。
